{
  "name": "jscpd-service",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "fix": "prettier --write .",
    "test": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
